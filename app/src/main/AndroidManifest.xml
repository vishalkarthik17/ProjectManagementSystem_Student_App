<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.student_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Select_Team"></activity>
        <activity android:name=".Role_Skill" />
        <activity android:name=".View_Approved" />
        <activity android:name=".Request_Budget" />
        <activity android:name=".Request_Resources" />
        <activity android:name=".Next_Review" />
        <activity
            android:name=".Home_Page"
            android:label="@string/title_activity_home__page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>